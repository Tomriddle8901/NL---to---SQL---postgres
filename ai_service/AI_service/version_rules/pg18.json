{
  "version": "18",
  "label": "PostgreSQL 18",
  "documentation_summary": [
    "Generate standard PostgreSQL SQL compatible with version 18.",
    "Use safe and explicit PostgreSQL syntax.",
    "Do not invent unsupported functions, operators, or clauses.",
    "Use only schema elements explicitly provided by the system."
  ],
  "prompt_rules": [
    "Target PostgreSQL 18 syntax only.",
    "Do not assume version-specific features unless verified.",
    "If the schema is insufficient, return: SELECT 'Unable to generate query';"
  ],
  "blocked_patterns": []
}